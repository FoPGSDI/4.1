# Notation & Conventions

*No custom commands or explicit convention blocks were detected.*

If your document uses specific conventions (metric signature, units, etc.),
consider adding a `%% BEGIN CONVENTIONS` / `%% END CONVENTIONS` block
in your LaTeX source, or a `\begin{conventions}` environment.